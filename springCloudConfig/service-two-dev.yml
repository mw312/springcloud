server:
  port: 8762

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

logging:
  level:
    com.murphy.springcloud: 'DEBUG'

test: this is from gitHub config center -- service two dev.

