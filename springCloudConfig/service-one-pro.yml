

spring:
  datasource:
    url: jdbc:postgresql://10.10.10.110:5432/tomato
    username: test
    password: test
    driver-class: org.postgresql.Driver

logging:
  level:
    com.murphy.springcloud: 'INFO'

test: this is from gitHub config center -- service-one-pro.yml --sf
