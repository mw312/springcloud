server:
  port: 8763

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/

spring:
  application:
    name: service-ribbon

service-one:
  domain: http://service-one #如果restTemplate没有配置@LoadBalanced，则需要填写服务的实际ip地址：http://localhost:8761。反之则配置服务别名。